FROM python:3.12-slim
WORKDIR /kehrnel
COPY . .
# Install kehrnel with runtime dependencies
RUN pip install --no-cache-dir -e .
CMD ["uvicorn", "kehrnel.api.app:app", "--host", "0.0.0.0", "--port", "9000"]
EXPOSE 9000
