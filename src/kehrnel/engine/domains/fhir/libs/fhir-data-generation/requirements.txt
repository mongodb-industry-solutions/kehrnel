# fhir-gen — all dependencies (Python 3.11+)
# Use only inside an activated virtual environment (.venv):
#   pip install -r requirements.txt

# --- Runtime ---
pydantic>=2.0,<3
pydantic-settings>=2.0,<3
pymongo>=4.6,<5
click>=8.1,<9
faker>=24.0,<37
PyYAML>=6.0,<7

# --- Development & testing ---
pytest>=7.4,<9
pytest-asyncio>=0.21,<1
pytest-cov>=4.1,<8
mongomock>=4.1,<5
ruff>=0.1,<1
mypy>=1.5,<2
