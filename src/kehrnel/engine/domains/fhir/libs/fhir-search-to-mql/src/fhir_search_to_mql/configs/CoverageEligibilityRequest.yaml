resource: CoverageEligibilityRequest
fhir_version: R5

# Structure: schema/indexes/resources.r5.json (resource_spec CoverageEligibilityRequest)
# Search params: schema/indexes/search-parameters.r5.json
# Compartments: src/fhir_search_to_mql/compartments/definitions/patient.json
#
# Total parameters: 9 (7 resource-specific per R5 index + 2 common:
# `_id`, `_lastUpdated`).
#
# R5 structural notes:
#   * `patient`, `insurer` are required Reference fields.
#   * `patient` search is the Patient compartment linking parameter.
#   * Top-level scalar `status` queries the FHIR root.
#   * `created` is dateTime at resource root.

search_parameters:
  # ─────────────────────────────────────────────────────────────
  # Date Parameters
  # ─────────────────────────────────────────────────────────────
  created:
    type: date
    description: "The creation date for the request"
    fields:
      - field: "created"
        type: date

  # ─────────────────────────────────────────────────────────────
  # Reference Parameters
  # ─────────────────────────────────────────────────────────────
  enterer:
    type: reference
    description: "The party who entered the request"
    fields:
      - field: "_search.entererId"
        referenceType: id
      - field: "_search.entererType"
        referenceType: type
      - field: "enterer.reference"
        referenceType: full

  facility:
    type: reference
    description: "Facility where services are intended to be provided"
    fields:
      - field: "_search.facilityId"
        referenceType: id
      - field: "_search.facilityType"
        referenceType: type
      - field: "facility.reference"
        referenceType: full

  patient:
    type: reference
    description: "The patient for whom eligibility is requested"
    fields:
      - field: "_search.patientId"
        referenceType: id
      - field: "patient.reference"
        referenceType: full

  provider:
    type: reference
    description: "The practitioner or organization provider"
    fields:
      - field: "_search.providerId"
        referenceType: id
      - field: "_search.providerType"
        referenceType: type
      - field: "provider.reference"
        referenceType: full

  # ─────────────────────────────────────────────────────────────
  # Token Parameters
  # ─────────────────────────────────────────────────────────────
  identifier:
    type: token
    description: "The business identifier of the eligibility request"
    fields:
      - field: "_search.identifier_systemCode"
        tokenType: systemCode
      - field: "_search.identifier_values"
        tokenType: code

  status:
    type: token
    description: "The status of the eligibility request"
    fields:
      - field: "status"
        tokenType: code

  # ─────────────────────────────────────────────────────────────
  # Common Parameters
  # ─────────────────────────────────────────────────────────────
  _id:
    type: token
    description: "Logical id of this artifact"
    fields:
      - field: "id"
        tokenType: code

  _lastUpdated:
    type: date
    description: "When the resource version last changed"
    fields:
      - field: "meta.lastUpdated"
        type: date

denormalization:
  identifier:
    source: identifier
    target: _search
    extractor: IdentifierExtractor
    field_mappings:
      - source_path: identifier[*].value
        target_field: identifier_values
        datatype: array[string]
      - source_path: identifier[*]
        target_field: identifier_systemCode
        datatype: array[string]

  patient:
    source: patient
    target: _search
    extractor: ReferenceExtractor
    field_mappings:
      - source_path: patient.reference
        target_field: patientId
        datatype: string
        extractType: id

  enterer:
    source: enterer
    target: _search
    extractor: ReferenceExtractor
    field_mappings:
      - source_path: enterer.reference
        target_field: entererId
        datatype: string
        extractType: id
      - source_path: enterer.reference
        target_field: entererType
        datatype: string
        extractType: type

  provider:
    source: provider
    target: _search
    extractor: ReferenceExtractor
    field_mappings:
      - source_path: provider.reference
        target_field: providerId
        datatype: string
        extractType: id
      - source_path: provider.reference
        target_field: providerType
        datatype: string
        extractType: type

  facility:
    source: facility
    target: _search
    extractor: ReferenceExtractor
    field_mappings:
      - source_path: facility.reference
        target_field: facilityId
        datatype: string
        extractType: id
      - source_path: facility.reference
        target_field: facilityType
        datatype: string
        extractType: type

  compartment_membership:
    source: $resource
    target: _compartments
    extractor: CompartmentMembershipExtractor
    field_mappings:
      - target_field: Patient
        source_paths:
          - patient
        reference_type: Patient
        datatype: array[string]
        comment: "Patient compartment (patient.json linking param patient)"

compartments:
  precomputed:
    - Patient

indexes:
  - fields:
      - "_compartments.Patient": 1
    options:
      name: idx_compartment_patient
      sparse: true

  - fields:
      - "status": 1
    options:
      name: idx_status

  - fields:
      - "_search.patientId": 1
    options:
      name: idx_patient_id
      sparse: true

  - fields:
      - "created": 1
    options:
      name: idx_created
      sparse: true

  - fields:
      - "meta.lastUpdated": 1
    options:
      name: idx_meta_last_updated
      sparse: true
