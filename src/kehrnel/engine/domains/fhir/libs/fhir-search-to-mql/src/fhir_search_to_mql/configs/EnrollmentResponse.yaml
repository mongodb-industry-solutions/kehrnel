resource: EnrollmentResponse
fhir_version: R5

# Structure: schema/indexes/resources.r5.json (resource_spec EnrollmentResponse)
# Search params: schema/indexes/search-parameters.r5.json
# Compartments: none — not listed in compartments/definitions/*.json
#
# Total parameters: 5 (3 resource-specific per R5 index + 2 common:
# `_id`, `_lastUpdated`).

search_parameters:
  identifier:
    type: token
    description: "The business identifier of the EnrollmentResponse"
    fields:
      - field: "_search.identifier_systemCode"
        tokenType: systemCode
      - field: "_search.identifier_values"
        tokenType: code

  request:
    type: reference
    description: "The enrollment request being responded to"
    fields:
      - field: "_search.requestId"
        referenceType: id
      - field: "request.reference"
        referenceType: full

  status:
    type: token
    description: "The status of the enrollment response"
    fields:
      - field: "status"
        tokenType: code

  _id:
    type: token
    description: "Logical id of this artifact"
    fields:
      - field: "id"
        tokenType: code

  _lastUpdated:
    type: date
    description: "When the resource version last changed"
    fields:
      - field: "meta.lastUpdated"
        type: date

denormalization:
  identifier:
    source: identifier
    target: _search
    extractor: IdentifierExtractor
    field_mappings:
      - source_path: identifier[*].value
        target_field: identifier_values
        datatype: array[string]
      - source_path: identifier[*]
        target_field: identifier_systemCode
        datatype: array[string]

  request:
    source: request
    target: _search
    extractor: ReferenceExtractor
    field_mappings:
      - source_path: request.reference
        target_field: requestId
        datatype: string
        extractType: id

indexes:
  - fields:
      - "status": 1
    options:
      name: idx_status

  - fields:
      - "_search.requestId": 1
    options:
      name: idx_request_id
      sparse: true

  - fields:
      - "_search.identifier_systemCode": 1
    options:
      name: idx_identifier_system_code
      sparse: true

  - fields:
      - "meta.lastUpdated": -1
    options:
      name: idx_meta_last_updated
